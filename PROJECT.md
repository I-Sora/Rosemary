# ROSEMARY

## DESCRIPTION

このプロジェクトは、小説・脚本等の創作を行う際に作成するストーリープロットを、プレーンテキストのみで記述するための記法 `ROSEMARY` を設計、作成するものである。

## 記法の用途

### 用途

小説・脚本等のストーリープロットの制作。

### 想定使用者

Markdown 記法に慣れた、プロ・アマチュアを問わない作家。

### 達成されるべき目的

- シーン・キャラクター・セリフの管理
- シーン・キャラクター・セリフ間の関係性の管理
- キャラクター情報とキャラクター間の関係性の管理
- 伏線・回収・未定義情報の管理
- プロット構造の階層的な整理
- 複数 `.rsmr` ファイルに分割された創作情報の統合

## ファイル拡張子

`.rsmr`

## 基本文法

以下に基本文法を示す。用いられている文字列のうち `""` で囲まれた文字列は、ユーザーが任意に指定する文字列であるとする。

### block

一つのシーン・セリフ・情報などを示す構造。内部に他の情報を持つが、基本形は以下である。

```
"block_name"{
    "text"
}
```

1 行で記述することもできる。

```
"block_name"{"text"}
```

### attribute

- `block` に付与され、そのブロックの属性を決定する。
- `attribute` を示す文字列はユーザーが任意に指定する。
- block 種別は `attribute` を用いて表現する。
- `attribute` は、単独の属性名と key-value 属性の両方を扱う。

```
"block_name"[scene]{
    "text"
}
```

key-value 属性は以下のように記述する。

```
"block_name"[scene, chapter=1, pov="ミオ", place="駅", status="draft"]{
    "text"
}
```

block 種別の例を以下に示す。

- `scene`: シーン
- `character`: キャラクター
- `dialogue`: セリフ
- `info`: 情報
- `item`: アイテム
- `act`: 幕
- `chapter`: 章
- `state`: 特定時点・期間における状態
- `relationship`: キャラクター間の関係
- `unknown`: 未定義・未決定の情報

未定義・未決定の情報は `unknown` 属性を用いて表現する。

```
"犯人の動機"[unknown]{
    "まだ未定"
}
```

### tag

- `tag` は、block を検索・分類するための補助的な印として用いる。
- `tag` は `#` から始まる文字列として記述する。
- 複数の tag を付与できる。

```
"古い鍵"[item][#伏線 #未回収 #父親]{
    "父の遺品。第三章で意味が判明する"
}
```

### include

- `include` は、他の `.rsmr` ファイルを読み込み、記述された `block` を現在のプロジェクト内で利用可能にする。
- `include` はトップレベルにのみ記述できる。
- `include` 対象のパスは、`include` を記述したファイルからの相対パスとして解釈する。
- 最初に対応する対象は単一ファイルのみとし、ワイルドカードは将来拡張とする。
- `include` は再帰的に解決する。
- 循環 `include` は error として診断し、その `include` は無視して解析を継続する。
- `include` 先の `block` は、`link` 解決、補完、定義ジャンプ、graph、Blocks 表示の対象に含める。
- 同名 `block` が複数ファイルに存在する場合は warning として診断する。

```
@include "character.rsmr"
@include "item.rsmr"

"第一幕"[act]{
    "雨の日の再会"[scene]{
        @ "佐倉ミオ"
        @ "古い鍵" --"発見"-->
    }
}
```

`character.rsmr` の例を以下に示す。

```
"佐倉ミオ"[character]{
    age: 17
    personality: "慎重"
}
```

### field

- `field` は、`block` 内に記述される任意の項目である。
- 項目名の後ろに `:` を置き、続けて値を記述する。
- 項目名はユーザーが任意に指定できる。
- 値には文字列、数値、真偽値などを指定できる。
- 長文、通常リスト、数字リスト、セリフ、注釈は `field: [` から単独行の `]` までを値として記述する。
- `field: [` 内では、通常の文章、`- ` で始まる通常リスト、`1. ` のような数字で始まる数字リスト、`speaker >line` のセリフ、`^` で始まる注釈を混在できる。
- 通常リストと数字リストは、インデントによって多重リストとして記述できる。
- 通常リスト、数字リスト、セリフの末尾には `^ 注釈` を付けられる。
- 文章として `- `、`1. `、`^` から始めたい場合は、`\- `、`\1. `、`\^` のように先頭をエスケープする。

```
"佐倉ミオ"[character]{
    age: 17
    personality: "慎重"
    goal: "父の失踪の真相を知る"
}
```

長文の例を以下に示す。

```
"雨の日の再会"[scene]{
    summary: [
        ミオと蓮が十年ぶりに駅で再会する。
        雨の音で会話が途切れ、互いに言えないことを抱えている。
        古い鍵はこの時点では意味を明かさない。
    ]
}
```

通常リスト、数字リストの例を以下に示す。

```
"雨の日の再会"[scene]{
    summary: [
        ミオと蓮が十年ぶりに駅で再会する。
        - "ミオが駅に到着する" ^ 動作は短く
            - "傘を閉じる"
            - "ホームを見渡す"
        - "蓮がミオに気づく"
        1. "再会"
            1. "沈黙"
            2. "確認"
        2. "沈黙" ^ 余白を作る
        ミオ >また会うとは思わなかった ^ 小さな声
        蓮 >俺もだ
        ^ 初稿では再会の余韻を長めに取る
        古い鍵はこの時点では意味を明かさない。
    ]
}
```

### link

- `link` は `block` 内に配置され、その `block` と指定する `block` の関係を示す。
- `block` の指定には `@ "block_name"` を用いる。
- `@` の後ろに半角スペースがなければ `link` として認識しない。
- `link` には `attribute` を付与できる。
- 描画される際は、記述元の `block` から指定先の `block` へ向けて矢印が表示される。

```
"block_A"{
    @ "block_B"
}

"block_B"{}
```

`link` に `attribute` を付与する場合は、指定先の `block` 名の後ろに記述する。

```
"佐倉ミオ"[character]{
    @ "高瀬蓮"[relation, type="幼なじみ", trust=6]
}
```

### line

- `line` は `link` に付与する指示として配置され、その `link` の矢印の種類や意味を決定する。
- 指定がなければデフォルトで実線になっている。
- 種類指定と文字列を組み合わせて、矢印の上に文字列を表示することもできる。
- 双方向に同様の関係を持つ場合は、双方向矢印を用いる。

指定できる矢印の種類と記述方法は以下の通りである。

- 実線: `-->`, `--"伏線"-->`
- 点線: `-.->`, `-."伏線"-.->`
- 太線: `==>`, `=="伏線"==>`
- 双方向実線: `<-->`, `<--"幼なじみ"-->`

```
"block_A"{
    @ "block_B" --"伏線"-->
}

"block_B"{}
```

双方向矢印は、記述元の `block` と指定先の `block` が同様の関係を持つ場合に用いる。

```
"佐倉ミオ"[character]{
    @ "高瀬蓮"[relation, type="幼なじみ"] <--"幼なじみ"-->
}
```

両者の感情・認識・信頼度などが異なる場合は、双方向矢印ではなく、片方向の `link` をそれぞれ記述する。

### 伏線と回収

- `?` は、謎・問い・未解決の論点を示す。
- `!` は、答え・回収・判明した事実を示す。
- `?` および `!` は `block` 内に記述する。
- 本文は引用符なしで記述できる。引用符付きの記述も引き続き許可する。

```
"古い鍵"{
    ? なぜ父はこれを残したのか
    ! 地下室を開ける鍵だった
}
```

### 階層構造

- `block` は他の `block` を内部に持つことができる。
- 階層構造は、幕・章・シーン・ビートなどの親子関係を表現するために用いる。

```
"第一幕"[act]{
    "主人公が事件に巻き込まれる"

    "第一章"[chapter]{
        "物語の導入"

        "雨の日の再会"[scene]{
            "ミオと蓮が再会する"
        }
    }
}
```

### セリフ・発話者

- `block` 内では、発話者名とセリフを組み合わせて記述できる。
- 発話者名の後ろに `>` を置き、続けてセリフ本文を記述する。
- `:` は field のために使い、セリフには使わない。

```
"雨の日の再会"[scene]{
    ミオ >久しぶり
    蓮 >……本当に、ミオなのか
}
```

### 注釈

- `^` は、本文や field 内に残す注釈を示す。
- `^` の後ろに続く文字列を注釈本文として扱う。
- 通常リスト、数字リスト、セリフの末尾には `^ 注釈` を付けられる。
- プレビュー上では斜字で表示する。

```
"雨の日の再会"[scene]{
    ^ 雨音を強めに描写する

    summary: [
        ミオと蓮が十年ぶりに駅で再会する。
        - ミオが駅に到着する ^ 動作は短く
        1. 再会 ^ 余白を作る
        ミオ >久しぶり ^ 小さな声
        ^ 初稿用の補足
    ]
}
```

### キャラクター定義

- キャラクターは `character` 属性を持つ `block` として定義する。
- キャラクター名は `block_name` として記述する。
- 年齢、性格、目的などの情報は `field` として記述する。
- `field` の項目名はユーザーが任意に指定できる。

```
"佐倉ミオ"[character]{
    age: 17
    personality: "慎重"
    goal: "父の失踪の真相を知る"
    weakness: "他人を信用できない"
}
```

#### キャラクター情報の時間変化

- 時間によるキャラクター情報の変化は、キャラクター `block` 内に `state` 属性を持つ `block` を記述して表現する。
- `state` には、特定時点を示す `at`、期間を示す `from` および `to` を指定できる。
- キャラクター直下の `field` は基本情報として扱う。
- `state` 内の `field` は、その時点・期間における情報として扱う。

```
"佐倉ミオ"[character]{
    age: 17
    personality: "慎重"
    goal: "父の失踪の真相を知る"

    "第一章時点"[state, at="第一章"]{
        personality: "他人を信用しない"
        goal: "父の手がかりを探す"
    }

    "第四章時点"[state, at="第四章"]{
        personality: "蓮だけは信用し始めている"
        goal: "父を追っていた組織の正体を暴く"
    }
}
```

期間を持つ状態は以下のように記述する。

```
"佐倉ミオ"[character]{
    personality: "慎重"

    "序盤の状態"[state, from="第一章", to="第二章"]{
        trust: "低い"
        personality: "警戒心が強い"
    }

    "中盤の状態"[state, from="第三章", to="第五章"]{
        trust: "揺れている"
        personality: "蓮にだけ弱さを見せる"
    }
}
```

#### キャラクター間の関係

- キャラクター間の関係は、`relation` 属性を持つ `link` として記述する。
- 関係の種類、信頼度、感情などは `link` の `attribute` として記述する。
- 関係に時間情報を持たせる場合は、`at`、`from`、`to` を用いる。
- 双方向に同様の関係を持つ場合は `<-->` または `<--"label"-->` を用いる。

```
"佐倉ミオ"[character]{
    @ "高瀬蓮"[relation, type="幼なじみ", trust=2, at="第一章"] --"不信"-->
    @ "高瀬蓮"[relation, type="協力者", trust=5, at="第三章"] --"共闘"-->
    @ "高瀬蓮"[relation, type="大切な人", trust=9, at="終章"] =="信頼"==>
}
```

双方向に同様の関係を持つ場合は以下のように記述する。

```
"佐倉ミオ"[character]{
    @ "高瀬蓮"[relation, type="協力者", trust=7, at="第四章"] <--"共闘"-->
}
```

両者で関係内容が異なる場合は、片方向の `link` をそれぞれ記述する。

```
"佐倉ミオ"[character]{
    @ "高瀬蓮"[relation, emotion="不信", trust=2] --"警戒"-->
}

"高瀬蓮"[character]{
    @ "佐倉ミオ"[relation, emotion="庇護", trust=8] --"守りたい"-->
}
```

関係そのものを詳細に管理したい場合は、`relationship` 属性を持つ独立した `block` として記述できる。

```
"ミオと蓮の関係"[relationship, from="佐倉ミオ", to="高瀬蓮"]{
    "第一章時点"[state, at="第一章"]{
        type: "幼なじみ"
        trust: 2
        emotion: "不信"
    }

    "第三章時点"[state, at="第三章"]{
        type: "協力者"
        trust: 5
        emotion: "共闘"
    }

    "終章時点"[state, at="終章"]{
        type: "大切な人"
        trust: 9
        emotion: "信頼"
    }
}
```

### コメント

- `//` から始まる行はコメントとして扱う。
- コメントは描画や関係性の解析には利用しない。

```
// このシーンは後で場所を変更する可能性がある
"雨の日の再会"[scene]{
    "駅で再会する"
    // ここで傘を共有させる
}
```

### メタ情報

- ファイル全体に関わる情報は、ファイル先頭のメタ情報として記述できる。
- メタ情報は `---` で囲む。

```
---
title: "雨の記憶"
genre: "ミステリ"
theme: "信頼と嘘"
version: 0.1
graph: true
---

"第一幕"[act]{
    "主人公が事件に巻き込まれる"
}
```

- `graph` はプレビュー上の Graph 表示を制御する。
- 未記述、または `graph: true` の場合は Graph を表示する。
- `graph: false` の場合は Graph を非表示にする。

## 描画方法

### block

- 看板のように描画する。
- `block_name`、`attribute`、`tag`、内容の文字列を表示する。

### link

- 片方向の `link` は、記述元の `block` から指定先の `block` へ向かう矢印として描画する。
- 双方向の `link` は、記述元の `block` と指定先の `block` を結ぶ双方向矢印として描画する。
- `line` が指定されている場合は、その種類に応じて線の描画方法を変更する。
- `line` に文字列が指定されている場合は、矢印の上にその文字列を表示する。
 
